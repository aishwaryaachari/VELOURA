DROP YOUR IMAGES HERE — names must match exactly:

HOME PAGE (transparent PNGs work best):
  hero-serums.png     → main hero visual (2-3 bottles + crystals on tray). Recommended 900×700.
  hero-zoom.png       → small circular inset. Recommended 400×400.
  feature-crystal.png → Features left card. Recommended 400×480.
  feature-serum.png   → Features right card. Recommended 400×480.

PRODUCTS (one per cosmeticId):
  products/101.png, products/102.png, ...  (transparent PNG, ~600×800)
  Fallback generics (optional): p1.png, p2.png, p3.png in this folder.

If a file is missing, the page auto-falls back to pure-CSS art — nothing breaks.
